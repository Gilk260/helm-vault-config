{{- range $name, $config := .Values.authEngineMounts }}
{{- if $config }}
---
apiVersion: redhatcop.redhat.io/v1alpha1
kind: AuthEngineMount
metadata:
  name: {{ include "vault-config.resourceName" (dict "context" $ "resourceName" $name "resourceType" "authenginemount") }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "vault-config.labels" $ | nindent 4 }}
spec:
  authentication:
    path: {{ default "kubernetes" $config.authentication.path | quote }}
    role: {{ required (printf "authEngineMounts.%s.authentication.role is required" $name) $config.authentication.role | quote }}
    serviceAccount:
      name: {{ default "default" ($config.authentication.serviceAccount).name | quote }}
    {{- with $config.authentication.namespace }}
    namespace: {{ . | quote }}
    {{- end }}
  {{- with $config.connection }}
  connection:
    address: {{ .address | quote }}
    {{- with .maxRetries }}
    maxRetries: {{ . }}
    {{- end }}
    {{- with .timeOut }}
    timeOut: {{ . | quote }}
    {{- end }}
    {{- with .tLSConfig }}
    tLSConfig:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  type: {{ required (printf "authEngineMounts.%s.type is required" $name) $config.type | quote }}
  path: {{ required (printf "authEngineMounts.%s.path is required" $name) $config.path | quote }}
  {{- with $config.description }}
  description: {{ . | quote }}
  {{- end }}
  {{- with $config.config }}
  config:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $config.local }}
  local: {{ . }}
  {{- end }}
  {{- with $config.sealwrap }}
  sealwrap: {{ . }}
  {{- end }}
  name: {{ default $name $config.name | quote }}
{{- end }}
{{- end }}
